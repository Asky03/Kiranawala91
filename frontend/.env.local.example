# Public — exposed to the browser (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-only — used by middleware.ts to verify JWT at the edge.
# MUST match backend's JWT_SECRET exactly.
JWT_SECRET=CHANGE_ME_must_match_backend_JWT_SECRET_exactly
